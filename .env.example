# OfficeRnD API Configuration
# These environment variables are required for fetching member and company data
# from the OfficeRnD Flex 2 API at build time.
#
# For Netlify deployment:
# Set these in Netlify UI: Site settings > Environment variables
# Or via Netlify CLI: netlify env:set VAR_NAME value

# OAuth 2.0 Client Credentials (from OfficeRnD Portal)
# Required: Your application's client ID
OFFICERND_CLIENT_ID=your_client_id_here

# Required: Your application's client secret
OFFICERND_CLIENT_SECRET=your_client_secret_here

# Required: Your organization's slug (found in the OfficeRnD URL)
OFFICERND_ORG_SLUG=your_org_slug_here

# Note: Token URL and API base URL are hardcoded in src/lib/api.ts:
# - Token endpoint: https://identity.officernd.com/oauth/token
# - API base: https://app.officernd.com/api/v2/organizations
